language: node_js
node_js:
  - "0.10"
before_install:
  - npm install -g yo grunt-cli bower
  - gem install haml
  - gem install sass
  - gem install compass
install:
  - npm install
  - bower install
before_script: grunt server